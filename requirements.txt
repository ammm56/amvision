# 推荐 Python 版本：3.12

# 基础依赖
fastapi[standard]>=0.136
SQLAlchemy>=2.0
pydantic>=2.13.2
pydantic-settings>=2.0
PyYAML>=6.0.3,<7
packaging>=24,<26
chardet>=5.2,<6
numpy>=2.2,<3
opencv-python>=4.11,<5
Pillow>=12.2.0,<13
regex>=2026.5.0,<2027
tensorboard>=2.19,<3
torch==2.12.1
torchvision==0.27.1
transformers>=5.9.0,<6
httpx2>=2.3,<3

# 开发工具依赖
pytest>=8.3,<9
ruff>=0.15.12,<0.20

# 数据库迁移
alembic>=1.18,<2

# COCO 数据集与评估
pycocotools>=2.0.8,<3
scipy>=1.17,<2

# RF-DETR full core 训练、数据集和评估
tqdm>=4.67,<5
albumentations>=2.0.8,<3
kornia>=0.8.3,<1
pytorch-lightning>=2.6.5,<3
torchmetrics>=1.9.0,<2

# ONNX 导出与简化
onnx>=1.18,<2
onnxscript>=0.7,<1
onnxsim>=0.4.36,<1
onnx-graphsurgeon>=0.6.1,<1
polygraphy>=0.50,<1

# ONNXRuntime 推理
onnxruntime>=1.22,<2

# openvino 推理
openvino>=2026.1.0 

# TensorRT 转换与推理
# 统一固定为 CUDA 12 发行包；版本必须与 runtimes/tensorrt_bin/ 中的 trtexec/DLL 对齐。
tensorrt-cu12==10.16.1.11
cuda-python==12.8.0

# 自定义节点依赖
zxing-cpp>=3.0,<4

# 图触发调用层依赖
pyzmq>=27.1.0,<30
watchfiles>=1.2,<2

# 可选
psutil>=7.2.2,<8

# 如需通过 PyTorch 官方 CUDA 12.6 源安装：
# 当前基线为 PyTorch 2.12.1 + CUDA 12.6，Windows 单机多 GPU DDP 使用 Gloo。
# pip install torch==2.12.1 torchvision==0.27.1 --index-url https://download.pytorch.org/whl/cu126




















