{
  "format_id": "amvision.node-definition.v1",
  "node_type_id": "custom.opencv.image-refs-empty-check",
  "display_name": "Image Refs Empty Check",
  "category": "opencv.analysis",
  "description": "消费 Image Refs Slot Metrics.summary，并按多指标规则输出 empty / non-empty 判断，适合批量槽位空槽检查。",
  "metadata": {
    "i18n": {
      "display_name": {
        "zh-CN": "图片集合空槽检查",
        "en-US": "Image Refs Empty Check"
      },
      "description": {
        "zh-CN": "消费 Image Refs Slot Metrics.summary，并按多指标规则输出 empty / non-empty 判断，适合批量槽位空槽检查。",
        "en-US": "Consume Image Refs Slot Metrics.summary and classify each slot as empty or non-empty with multi-metric rules."
      }
    }
  },
  "implementation_kind": "custom-node",
  "runtime_kind": "python-callable",
  "input_ports": [
    {
      "name": "images",
      "display_name": "Images",
      "payload_type_id": "image-refs.v1",
      "description": "可选调试图源，通常来自 Crop Export.crops；只在编辑态交互式参数图片面板和 contact sheet 中使用。",
      "required": false
    },
    {
      "name": "metrics",
      "display_name": "Metrics",
      "payload_type_id": "value.v1",
      "description": "必填的 Image Refs Slot Metrics.summary。空槽检查只按已有指标做规则判断，不重复计算图像指标。",
      "required": true
    }
  ],
  "output_ports": [
    {
      "name": "summary",
      "display_name": "Summary",
      "payload_type_id": "value.v1",
      "description": "每张图片的指标、规则判断、empty / non-empty 结果和整批摘要。"
    },
    {
      "name": "body",
      "display_name": "Body",
      "payload_type_id": "response-body.v1",
      "description": "可作为 workflow HTTP 输出的批量空槽检查结果 body。"
    },
    {
      "name": "debug_preview",
      "display_name": "Debug Preview",
      "payload_type_id": "response-body.v1",
      "description": "仅编辑 Preview Run 使用的空槽检查 contact sheet 和交互式调参面板。"
    }
  ],
  "parameter_schema": {
    "type": "object",
    "properties": {
      "expected_count": {
        "type": "integer",
        "minimum": 1,
        "title": "Expected Count",
        "description": "期望图片数量；配置后数量不一致会让整批状态为 failed。"
      },
      "std_gray_empty_max": {
        "type": "number",
        "minimum": 0,
        "default": 56,
        "title": "Std Gray Empty Max",
        "description": "空槽允许的最大灰度标准差；用于容忍空槽纹理，同时拦截有料区域的强变化。"
      },
      "dark_ratio_empty_max": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.02,
        "title": "Dark Ratio Empty Max",
        "description": "空槽允许的最大暗像素比例；用于发现黑色、深色或遮挡物料。"
      },
      "bright_ratio_empty_min": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "title": "Bright Ratio Empty Min",
        "description": "空槽要求的最小亮像素比例；留空时不启用该规则。"
      },
      "edge_density_empty_max": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.18,
        "title": "Edge Density Empty Max",
        "description": "空槽允许的最大边缘密度；用于发现插槽内额外结构、物料边缘或错位遮挡。"
      },
      "dark_component_area_ratio_empty_max": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.02,
        "title": "Dark Component Area Ratio Empty Max",
        "description": "空槽允许的最大暗连通域总面积比例；用于判断物料暗区域总体占比。"
      },
      "largest_dark_component_area_ratio_empty_max": {
        "type": "number",
        "minimum": 0,
        "maximum": 1,
        "default": 0.012,
        "title": "Largest Dark Component Ratio Empty Max",
        "description": "空槽允许的最大单个暗连通域面积比例；用于拦截单个大块物料或遮挡。"
      },
      "empty_when_all_rules_pass": {
        "type": "boolean",
        "default": true,
        "title": "All Rules Required",
        "description": "为 true 时所有启用规则都通过才判 empty；为 false 时任一规则通过即判 empty。"
      },
      "debug_image_panel_enabled": {
        "type": "boolean",
        "title": "显示调试图",
        "description": "仅编辑 Preview Run 使用；生产 runtime 默认不会生成调试图片。",
        "default": false
      },
      "debug_image_panel_transport_mode": {
        "type": "string",
        "title": "调试图返回方式",
        "description": "storage-ref 会保存为 Preview Run artifact；inline-base64 只适合小图快速调试。",
        "enum": [
          "storage-ref",
          "inline-base64"
        ],
        "default": "storage-ref"
      }
    }
  },
  "capability_tags": [
    "opencv.statistics",
    "inspection.batch",
    "inspection.empty-check",
    "inspection.slot"
  ],
  "runtime_requirements": {
    "python_packages": [
      "opencv-python",
      "numpy"
    ]
  },
  "node_pack_id": "opencv.basic-nodes",
  "node_pack_version": "0.1.0"
}
